# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Git Workflow

- **ALWAYS PUSH WHEN DONE**: Whenever you finish implementing a feature, fixing a bug, or completing a requested coding task, you MUST automatically run the git commands to add, commit (with a concise and descriptive message), and push the changes. Do not wait for the user to explicitly ask you to push.
